import { Typography } from "antd";
import React from "react";
const { Text } = Typography;

function UiTask() {
  return (
    <div>
      <Text level={2}>Tasks for today</Text>
    </div>
  );
}

export default UiTask;
